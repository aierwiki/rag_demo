## 角色定位：
    你是一个资深的测试工程师，接下来我会给你一个接口，你需要设计一些测试用例去测试这个接口。

## 任务描述
我将会提供一个函数接口的定义，包括接口的功能、接口的定义、接口的参数说明、接口的返回值。
接口的返回值通常为一个DataFrame,DataFrame中有多个列。
你在设计测试用例的时候，要考虑组合不同的输入和输出。
- 不同的输入可以通过阅读接口的参数说明来设计。
- 不同的输出指的是测试用例应该测试接口返回DataFrame的不同列，每个单独的列以及不同的多个列的组合。

### 输出格式
你的输出应该是一个json列表，列表中的每一项是一个测试用例，每个测试用例是一个字典，每个测试用例包含如下字段：
- query: 一个字符串，用自然语言来描述要测试的功能，要包含完整的信息，也就是说要达到能根据你的描述知道输入参数是什么样的，输出是什么样的。
- 输入参数: 一个字典，包含每个参数的名称和值。
- 输出：一个列表，表示要测试输出的列，每个元素是一个字符串，表示要测试的列的名称。

以下是接口的信息：
```
- query_history_k_data_plus
    - 接口功能
        - 获取A股历史交易数据。
        - 支持日、周、月K线数据以及5分钟、15分钟、30分钟和60分钟K线数据。
        - 适合搭配均线数据进行选股和分析
    - 接口定义：def query_history_k_data_plus(code:str, fields:str, start_date:str=None, end_date:str=None, frequency:str='d', adjustflag:str='3') -> pd.DataFrame
    - 参数说明
        - code: 股票代码，格式为"sh或sz.+6位数字代码"，或者指数代码，如："sh.601398"。此参数不可为空。
        - fields: 需要返回的字段列表，支持多指标输入，以半角逗号分隔。详细指标列表见“历史行情指标参数”章节，日线与分钟线参数不同。此参数不可为空。
        - start_date: 开始日期（包含），格式为"YYYY-MM-DD"。为空时，默认为2015-01-01。
        - end_date: 结束日期（包含），格式为"YYYY-MM-DD"。为空时，默认为最近一个交易日。
        - frequency: 数据类型，默认为"d"（日K线）。其他选项包括"w"（周K线）、"m"（月K线）、"5"（5分钟K线）、"15"（15分钟K线）、"30"（30分钟K线）、"60"（60分钟K线）。不区分大小写。指数没有分钟线数据。
        - adjustflag: 复权类型，默认不复权（3）。1表示后复权，2表示前复权。已支持分钟线、日线、周线、月线前后复权。
    - 返回值
        - 返回值类型为pandas的DataFrame类型，DataFrame中的列如下：
            | 参数名称 | 参数描述 | 算法说明 |
            | --- | --- | --- |
            | date | 交易所行情日期 |  |
            | code | 证券代码 |  |
            | open | 开盘价 |  |
            | high | 最高价 |  |
            | low | 最低价 |  |
            | close | 收盘价 |  |
            | preclose | 前收盘价 | 见表格下方详细说明 |
            | volume | 成交量（累计 单位：股） |  |
            | amount | 成交额（单位：人民币元） |  |
            | adjustflag | 复权状态(1：后复权， 2：前复权，3：不复权） |  |
            | turn | 换手率 | \( \text{指定交易日的成交量(股)} / \text{指定交易日的股票的流通股总股数(股)} \)\*100% |
            | tradestatus | 交易状态(1：正常交易 0：停牌） |  |
            | pctChg | 涨跌幅（百分比） | 日涨跌幅= \((\text{指定交易日的收盘价} - \text{指定交易日前收盘价}) / \text{指定交易日前收盘价}\)\*100% |
            | peTTM | 滚动市盈率 | \((\text{指定交易日的股票收盘价} / \text{指定交易日的每股盈余TTM}) = (\text{指定交易日的股票收盘价} * \text{截至当日公司总股本}) / \text{归属母公司股东净利润TTM}\) |
            | pbMRQ | 市净率 | \((\text{指定交易日的股票收盘价} / \text{指定交易日的每股净资产}) = \text{总市值} / (\text{最近披露的归属母公司股东的权益} - \text{其他权益工具})\) |
            | psTTM | 滚动市销率 | \((\text{指定交易日的股票收盘价} / \text{指定交易日的每股销售额}) = (\text{指定交易日的股票收盘价} * \text{截至当日公司总股本}) / \text{营业总收入TTM}\) |
            | pcfNcfTTM | 滚动市现率 | \((\text{指定交易日的股票收盘价} / \text{指定交易日的每股现金流TTM}) = (\text{指定交易日的股票收盘价} * \text{截至当日公司总股本}) / \text{现金以及现金等价物净增加额TTM}\) |
            | isST | 是否ST股，1是，0否 |  |

            注意“前收盘价”说明：
            证券在指定交易日行情数据的前收盘价，当日发生除权除息时，“前收盘价”不是前一天的实际收盘价，而是根据股权登记日收盘价与分红现金的数量、配送股的数里和配股价的高低等结合起来算出来的价格。
```


请按照要求给出你的测试用例。